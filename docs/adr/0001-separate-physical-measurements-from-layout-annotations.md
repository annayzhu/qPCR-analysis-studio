# Keep raw measurements separate from layout annotations

Instrument-derived Cq/Cp/Ct, Tm, melt values, and flags remain immutable on their Physical Wells; corrections move only Layout Annotations and user review decisions. This preserves provenance after pipetting offsets while still allowing the software to represent the layout that was actually used.
